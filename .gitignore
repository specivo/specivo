# Python
__pycache__/
*.py[cod]
*.egg-info/
# Python packaging artifacts (root only — do NOT ignore specivo/static/dist/,
# which holds the committed esbuild bundles served at runtime).
/dist/
build/
.eggs/

# Virtual environments (uv creates .venv/)
.venv/
venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment secrets (never commit)
.env
.env.local

# Data (persistent, not committed)
data/
specivo-data/
specivo-dev-data/
.bootstrap.json

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# Visual regression — actual diffs (baselines are committed)
tests/e2e/snapshots/*.actual.png

# OS
.DS_Store
Thumbs.db
.venv/
__pycache__/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
data/
specivo-data/
htmlcov/
specivo/static/models/
docs-site/

# Frontend build (esbuild) — node_modules is dev-only; the built bundles under
# specivo/static/dist/ ARE committed.
frontend/node_modules/
